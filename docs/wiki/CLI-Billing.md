<!-- Code generated by vngcloud gen-docs. DO NOT EDIT. -->

# CLI: Billing

## create-budget

Kind: Write.

| Flag | Type | Required |
|-|-|-|
| `--name` | `string` | yes |
| `--period-type` | `string` | yes |
| `--type` | `string` | yes |
| `--limit-amount` | `int64` | yes |
| `--status` | `string` |  |

```sh
vngcloud billing create-budget --name <name> --period-type <period-type> --type <type> --limit-amount <limit-amount>
```

## create-budget-threshold

Kind: Write.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |
| `--threshold-type` | `string` | yes |
| `--threshold-percentage` | `int` | yes |
| `--max-alerts-per-period` | `int` |  |
| `--reminder-interval-hours` | `int` |  |

```sh
vngcloud billing create-budget-threshold --budget-uuid <budget-uuid> --threshold-type <threshold-type> --threshold-percentage <threshold-percentage>
```

## delete-budget

Kind: Write, destructive.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |

```sh
vngcloud billing delete-budget --budget-uuid <budget-uuid> --yes
```

## delete-budget-threshold

Kind: Write, destructive.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |
| `--threshold-uuid` | `string` | yes |

```sh
vngcloud billing delete-budget-threshold --budget-uuid <budget-uuid> --threshold-uuid <threshold-uuid> --yes
```

## get-balances

Kind: Read.

No fields.

```sh
vngcloud billing get-balances --query Balances
```

## get-budget

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |

```sh
vngcloud billing get-budget --budget-uuid <budget-uuid> --query Budget
```

## get-cost-overview

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--start-date` | `string` | yes |
| `--end-date` | `string` | yes |
| `--product` | `string` |  |
| `--resource-type` | `string` |  |
| `--resource-id` | `string` |  |
| `--search` | `string` |  |
| `--group-by` | `string` |  |
| `--interval` | `string` |  |

```sh
vngcloud billing get-cost-overview --start-date <start-date> --end-date <end-date>
```

## get-current-period-cost

Kind: Read.

No fields.

```sh
vngcloud billing get-current-period-cost --query PeriodCost
```

## list-budget-alerts

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |
| `--period-key` | `string` |  |

```sh
vngcloud billing list-budget-alerts --budget-uuid <budget-uuid>
```

## list-budget-thresholds

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |

```sh
vngcloud billing list-budget-thresholds --budget-uuid <budget-uuid>
```

## list-budgets

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--status` | `string` |  |

```sh
vngcloud billing list-budgets
```

## list-cost-resources

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--start-date` | `string` | yes |
| `--end-date` | `string` | yes |
| `--product` | `string` |  |
| `--resource-type` | `string` |  |
| `--resource-id` | `string` |  |
| `--search` | `string` |  |
| `--page` | `int` |  |
| `--size` | `int` |  |
| `--sort` | `string` |  |
| `--order` | `string` |  |

```sh
vngcloud billing list-cost-resources --start-date <start-date> --end-date <end-date>
```

## update-budget

Kind: Write.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |
| `--name` | `*string` |  |
| `--period-type` | `*string` |  |
| `--type` | `*string` |  |
| `--limit-amount` | `*int64` |  |
| `--status` | `*string` |  |

```sh
vngcloud billing update-budget --budget-uuid <budget-uuid>
```

## update-budget-threshold

Kind: Write.

| Flag | Type | Required |
|-|-|-|
| `--budget-uuid` | `string` | yes |
| `--threshold-uuid` | `string` | yes |
| `--threshold-percentage` | `*int` |  |
| `--enabled` | `*bool` |  |
| `--max-alerts-per-period` | `*int` |  |
| `--reminder-interval-hours` | `*int` |  |

```sh
vngcloud billing update-budget-threshold --budget-uuid <budget-uuid> --threshold-uuid <threshold-uuid>
```

