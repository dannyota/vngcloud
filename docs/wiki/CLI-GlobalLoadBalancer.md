<!-- Code generated by vngcloud gen-docs. DO NOT EDIT. -->

# CLI: GlobalLoadBalancer

## get-listener

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |
| `--listener-id` | `string` | yes |

```sh
vngcloud globalloadbalancer get-listener --load-balancer-id <load-balancer-id> --listener-id <listener-id> --query Listener
```

## get-load-balancer

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |

```sh
vngcloud globalloadbalancer get-load-balancer --load-balancer-id <load-balancer-id> --query LoadBalancer
```

## get-pool-member

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |
| `--pool-id` | `string` | yes |
| `--pool-member-id` | `string` | yes |

```sh
vngcloud globalloadbalancer get-pool-member --load-balancer-id <load-balancer-id> --pool-id <pool-id> --pool-member-id <pool-member-id> --query PoolMember
```

## list-listeners

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |

```sh
vngcloud globalloadbalancer list-listeners --load-balancer-id <load-balancer-id>
```

## list-load-balancers

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--name` | `string` |  |
| `--offset` | `int` |  |
| `--limit` | `int` |  |

```sh
vngcloud globalloadbalancer list-load-balancers
```

## list-packages

Kind: Read.

No fields.

```sh
vngcloud globalloadbalancer list-packages
```

## list-pool-members

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |
| `--pool-id` | `string` | yes |

```sh
vngcloud globalloadbalancer list-pool-members --load-balancer-id <load-balancer-id> --pool-id <pool-id>
```

## list-pools

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |

```sh
vngcloud globalloadbalancer list-pools --load-balancer-id <load-balancer-id>
```

## list-regions

Kind: Read.

No fields.

```sh
vngcloud globalloadbalancer list-regions
```

## list-usage-histories

Kind: Read.

Unverified live: the test account has no global load balancer, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--load-balancer-id` | `string` | yes |
| `--from` | `string` |  |
| `--to` | `string` |  |
| `--type` | `string` |  |

```sh
vngcloud globalloadbalancer list-usage-histories --load-balancer-id <load-balancer-id>
```

