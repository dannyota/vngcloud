<!-- Code generated by vngcloud gen-docs. DO NOT EDIT. -->

# CLI: Volume

## get-underlying-volume

Kind: Read.

Unverified live: the test account has no volume, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--volume-id` | `string` | yes |

```sh
vngcloud volume get-underlying-volume --volume-id <volume-id> --query Volume
```

## get-volume

Kind: Read.

Unverified live: the test account has no volume, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--volume-id` | `string` | yes |

```sh
vngcloud volume get-volume --volume-id <volume-id> --query Volume
```

## get-volume-type

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--volume-type-id` | `string` | yes |

```sh
vngcloud volume get-volume-type --volume-type-id <volume-type-id> --query VolumeType
```

## list-all-snapshots

Kind: Read.

No fields.

```sh
vngcloud volume list-all-snapshots
```

## list-encryption-types

Kind: Read.

No fields.

```sh
vngcloud volume list-encryption-types
```

## list-snapshots

Kind: Read.

Unverified live: the test account has no volume, so this output shape comes from GreenNode's official SDK, not a live capture.

| Flag | Type | Required |
|-|-|-|
| `--volume-id` | `string` | yes |
| `--page` | `int` |  |
| `--size` | `int` |  |

```sh
vngcloud volume list-snapshots --volume-id <volume-id>
```

## list-volume-type-zones

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--zone-id` | `string` |  |

```sh
vngcloud volume list-volume-type-zones
```

## list-volume-types

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--volume-type-zone-id` | `string` |  |

```sh
vngcloud volume list-volume-types
```

## list-volumes

Kind: Read.

| Flag | Type | Required |
|-|-|-|
| `--name` | `string` |  |
| `--page` | `int` |  |
| `--size` | `int` |  |

```sh
vngcloud volume list-volumes
```

